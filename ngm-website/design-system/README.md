# Design System

Drop `NGM-design-system-prompt.md` here — the synthesized reference you paste at the
top of a new AI session.

**Quick reference**
- Colors: Sage `#3E5538` / `#5C7A52`, Terracotta Rose `#B87260`, Cream `#FAF7F2`, Linen, Charcoal `#242424` (all as `--ngm-` tokens)
- Type: Cormorant Garamond (display), DM Sans (body)
- Prefix: classes `.ngm-`, tokens `--ngm-`
- Italic `em` accents in headings: same size/weight as surrounding text, differ only by color + italic
