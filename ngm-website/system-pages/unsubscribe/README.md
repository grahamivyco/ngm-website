# Unsubscribe from emails

**WA system page:** Unsubscribe from emails

Reached from the unsubscribe link in email footers. Paste this ABOVE the native Submit / Cancel controls and the address list (replacing the old CAUTION graphic). The system buttons below are skinned by global.css.

- `01-top.slim.html` &mdash; markup only; styling comes from the Global CSS (must be live in WA).
- `01-top.slim.html` &mdash; relies on the `.ngm-sys` skin in `global-css/global.css`; paste this at cutover.

The `.ngm-sys` intro-band skin lives in the master `global.css` (SYSTEM-PAGE INTRO BANDS section).
