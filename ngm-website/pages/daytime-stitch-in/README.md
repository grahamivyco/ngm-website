# Daytime Stitch-In

**WA page:** Daytime Stitch-In  ·  **URL:** `/daytime-stitch-in`  (header Meetings menu)

Sandwich, one layout column:
1. `01-top.html` — Custom HTML (hero, details, upcoming heading + event-tagging script)
2. Native WA "Upcoming events" gadget — class `ngm-wa-events`, filtered to this group (see `02-wa-gadget.txt`)
3. `03-bottom.html` — Custom HTML (Visit section, Zoom tiles, join CTA)

Styling comes from `global-css/global.css` (must be live in the WA CSS tab first).
