# Membership Renewal

**WA page:** `/Sys/Profile (renewal flow)` — CSS-only (Wild Apricot does not allow custom HTML here).

This page is skinned entirely by `global-css/global.css` (the WA SYSTEM-PAGE FORMS +
WaGadgetLoginForm sections): DM Sans / Cormorant fonts, sage buttons, and NGM field
styling. There are no gadgets to paste for this page.

⚠️ **Provisional:** the skin targets WA's shared form classes but was not verified
against this specific live page (it requires login). After the Global CSS is live,
open `/Sys/Profile (renewal flow)`, check it, and if a field or button looks off, capture the element's
class and the skin can be tuned — same process used for the login page.
