# Donate

**URL:** `/donate`  ·  single self-contained Custom HTML gadget (`01-top.html`).
The 'Donate now' buttons point at /donation — repoint them at your Wild Apricot donation form.

After `global.css` is live, use `01-top.slim.html`.
